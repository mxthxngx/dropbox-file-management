export enum ButtonVariantType {
    Link = "link",
    Default = "default",
    Destructive = "destructive",
    Outline = "outline",
    Secondary = "secondary",
    Ghost = "ghost",
  }

export interface FileItem {
    id: string;
    name: string;
  }